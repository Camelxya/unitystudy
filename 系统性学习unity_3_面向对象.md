## *C#面向对象专栏*

#### Class 1 编程思想

###### 1\.1\. 面向过程

	以步骤为中心，将问题拆解成线性执行的函数或过程

	优点：	逻辑直观，适合简单任务

	缺点：	代码冗余，耦合度高，难以应对需求变化

###### 1\.2\. 面向对象

	以对象为中心，将问题借助面向对象的特性（封装、继承、多态）进行模块化设计

	优点：	模块化设计、拓展性强、降低系统复杂度，适合大型项目开发

	缺点：	性能略低，学习成本高

#### Class 2 类 Class

###### 2\.1\.类的基本概念

	类 Clase 是对现实事物的抽象模板，定义了同类对象的属性（数据）和方法（行为）；
	
	类是实现面向对象最基本的操作单元；

	对象（Object）是类创建的具体实例，可以独立操作和使用这个类。

	定义方法如下：
	(访问修饰符) class (对象名)
	{
		//变量
		//方法（构造函数、方法）
	}

###### 2\.2\.类的组成部分

	变量：

		基本概念：变量是程序中用于标识和操作存储区的名称，作为存储和表示数据的基本单元

		变量分类{
			局部变量{
				定义：在一个代码块里面声明的变量
				归属：属于当前所属的作用域，出了作用域就会被销毁
				}
			成员变量{
				定义：声明在类内部但非静态方法外，属于对象实例的属性
				归属：每个对象实例独立拥有成员变量副本
			}
			静态变量{
				定义：用static声明的变量
				归属：属于整个类，全局唯一，所有对象共享同一个静态变量
			}
			常量{
				定义：使用const关键字声明的变量，在编译时确定且不可修改，可以直接用类名访问
				归属：从编译时确定，伴随整个程序周期
			}
		}

	方法：

		基本概念：定义对象能执行的动作

		方法分类{
			成员方法：在类内定义的普通方法，可在方法内部通过this关键字（指向当前的对象实例，可选）访问成员变量或成员方法
			静态方法{
				概念{
					定义：用static关键字定义的方法，属于类本身
					调用方法：直接通过类名调用，无需通过实例化对象
					内存管理：静态方法在内存中仅存一份副本，生命周期与应用程序域绑定
					}
				注意{
					与成员方法的区别{
						访问权限：只能访问静态成员
						this关键字：静态方法中不可使用this，因其不关联任何实例
						静态方法里不能调用成员方法
					}
				}
			}
		}

	构造函数：
		
		基本介绍{
			用于创建对象时初始化属性
			若未显示定义构造函数，编译器会自动为类生成一个默认的无参构造函数
			通过new关键字触发构造函数执行，无法手动调用构造函数
		}
		书写要求{
			构造函数名称必须与类名相同
			不允许有返回值（void也不行）
			可通过参数数量、类型或顺序定义多个构造函数（也可构成函数重载）
		}
		分类{
			普通构造函数
			私有构造函数：用private声明，意味着只能在类的内部进行对类对象的实例化
			静态构造函数{
				作用：初始化静态成员变量
				触发机制{
					在创建类实例或访问静态成员（包括字段、方法、属性），自动调用
					静态构造函数先于实例构造函数执行，确保静态成员已初始化完毕
				}
				注意事项{
					无参数且不可重载
					也不可添加访问修饰符
					无论创建多少实例或访问多少次静态成员，静态构造函数仅执行一次
				}
			}
		}

###### 2\.3\.类中函数重载

	基本概念：允许在同一作用域内定义多个同名方法用来提升代码可读性

	触发的充分条件（任一即可）{
		参数类型不同
		参数数量不同
		参数顺序不同
	}
	特别注意{
		返回值无关
		访问修饰符无关
		作用域限制：重载方法必须定义在同一类或结构体内
		C#9.0以上允许泛型参数差异实现重载（不需要在面试说这个）
	}

###### 2\.4\.类的属性访问器

	属性通过get和set访问器封装类的私有字段，控制外部对类成员的安全访问和修改
		get 访问器：用于返回属性值，可实现只读效果，它是可选的
		set 访问器：用于设置属性值，它也是可读的
	用于添加私有字段的代码逻辑的检测

	封装是为了通过属性去限制数据的改变：
	public class Person
	{
		private string name;      // 字段（变量）——存储数据
		public string Name        // 属性——封装字段
		{
			get { return name; }          // 读取方法
			set { name = value; }         // 写入方法
		}
	}

	其中public string Name是属性而非变量，其下包含两个逻辑get和set，
	value是一个关键字，用来获取输入
	在 Person p = new Person() 下
	当出现
		p.Name = "Alice";	编译器会判断p.Name在等号左侧，所以转变为p.set_Name("Alice");
		string s = p.Name;	编译器会判断p.Name在等号右侧，所以转变为string s = p.get_Name();

	你可以借助属性给一个变量值的更改做出限定
	private string name;
	public string Name
	{
		get { return name; }
		set
	   {
			if (string.IsNullOrWhiteSpace(value))
				throw new ArgumentException("姓名不能为空");
			if (value.Length > 50)
				throw new ArgumentException("姓名过长");
			name = value;
		}
	}
	在这串封装中，大于50单位，和空集不能被输入

	private要求，你必须通过类内的方法去修改参数，而不能直接赋值修改

###### 2\.5\.命名规范

	类、接口、静态变量、方法	首字母大写，无间隔符				接口以I开头，私有方法前加_
	普通变量					首字母大写，后续单词首字母大写		public以m前缀，protected以m_，private以_
	局部变量					首字母小写，后续单词首字母大写
	常量						全大写字母+下划线分割

#### Class 3 面向对象的三大特征

###### 3\.1\.继承

	继承是面向对象编程的核心特征之一，允许派生类（子类）继承基类（父类）的成员（属性和方法）
	派生类自动继承基类的非私有字段（属性、字段、方法）
	仅支持单继承，但支持通过接口实现多重继承

	作用：
		可读性
		拓展性

	this 在构造函数中的应用，一个构造函数后加:this()，表明继承本类的另个一个构造函数，而初始化的值等于this中输入的值
	public class Animal
	{
		public string Name;
		public int Age;

		// 构造函数 A
		public Animal() : this("无名")   // 调用构造函数 B
		{
			Console.WriteLine("无参构造");
		}

		// 构造函数 B
		public Animal(string name) : this(name, 0)   // 调用构造函数 C
		{
			Console.WriteLine("一个参数构造");
		}

		// 构造函数 C
		public Animal(string name, int age)
		{
			Name = name;
			Age = age;
			Console.WriteLine("两个参数构造");
		}
	}
	this调用构造函数的时候，是按照输入的函数类型来确定指向哪个构造函数的，相当于函数重载
	先执行this，再执行该构造函数内新增的部分

	base和this注意点相同，唯一区别是base是调用父类的

	基类，可以直接用派生类的构造函数定义，因为安全，并且其格式完全是派生类格式，包括新增字段等

	public class Warrior : GameCharacter
	{
		private int _Strength;
		public int strength
		{
			get
			{
				return _Strength;
			}
			set 
			{
				if (value >= 0)
				{
					_Strength = value;
				}
			}
		}
		public Warrior(string n, int h, int s) :base(n, h)
		{
			strength = s;
		}
	}

###### 3\.2\.封装

	隐藏对象内部的实现细节，仅对外暴露必要的操作接口

	作用：
		数据安全，简化使用

	访问修饰符{
		public				公开访问
		private				私有访问，仅限当前类内
		protected			保护访问，允许本类及其派生类访问
		internal			同一程序集内可访问
		protected internal	满足两者任意一项即可
	}

###### 3\.3\.多态

	重写，在基类中创建一个virtual关键字的方法，在派生类中用override重新定义一个同名方法，此时在派生类中调用的就是派生类中的同名方法了

	基类，可以直接用派生类的构造函数定义，因为安全，并且其格式完全是派生类格式，包括新增字段等，并且也是调用派生类内的override方法

#### Class 4 更多的类
	
	静态类和静态方法{
		静态的关键字是static，
		静态类不能被实例化，不能被继承或继承，只能包含静态方法，
		一般用作方法库
	}
	抽象类和抽象方法{
		抽象的关键字是abstract，
		抽象类不能实例化，只能用作继承，
		抽象方法只能放在抽象类中，但抽象类可以盛放普通方法，
		只有抽象类中的所有抽象对象都被重写（也就是实现），抽象类的子类才能实例化
		一般用作通用的方法或实例的承载库
	}
	密封类和密封方法{
		密封的关键字是sealed，
		密封类不可被继承，密封方法不能被重写，
		密封方法可用于在某个层级的子类中，把某一个virtual方法用sealed密封，来使得这个子类下的所有次子类都不能重写该方法
	}
	泛型类和泛型方法{
		在类名后追加<字母>，在方法后追加<字母>
		增加了数据类型的灵活性
		类型参数在实例化类时，编译器完整的具体类型替代
	}

#### Class 5 接口Interface

###### 5\.1\.接口的基本概念

	接口是一种定义代码契约的机制，只声明方法或属性，但不提供具体的实现细节

	接口的关键字时interface，命名规则以I开头

	public interface IAnimal
	{
		string Name{get;set}
		void Eat();
	}
	
	注意{
		无实现逻辑，接口只定义能做什么，不定义如何实现
		多继承：一个类可以继承多个接口
		隐式公开：接口成员默认是public，并且不能添加访问修饰符
		无字段：不能有变量，但可以包含属性、方法、事件、索引器
	}

###### 5\.2\.接口的作用

	实现多态：通过接口类型（引用对象），调用统一的方法发

	解耦代码

###### 5\.3\.接口和类的区别

	接口只定义行为契约，仅定义方法、属性等成员的签名，不提供实现（即，没有方法，包括构造函数）

	接口不可直接实例化，但可以用接口声明对象，而靠继承其接口的类来实例化

	接口内不可包含字段，但可以包含属性

	一个类可以继承多个接口

#### Class 6 结构体Struct

###### 6\.1\.结构体的基本概念
	
	结构体是Csharp中的值类型，用于封装一组相关的数据（如坐标、颜色等）
	关键字是stuct，由此定义结构体
	表示轻量级、不可变的数据集合（如int、double的组合）

	使用场景：高频创建的小型数据类型

###### 6\.2\.结构体的定义

	public struct Monster{
	}

	结构体只能继承接口，可以继承实现多个接口（类是不能继承结构体的）

	结构体的成员变量只能在构造函数中初始化
	而结构体中的静态变量可以在声明时初始化

	结构体的构造函数必须含参数，不能定义默认无参构造函数

	结构体可以用new，也可以只声明不实例化（因为它储存在栈里，是值类型，声明也会开辟所有空间）

