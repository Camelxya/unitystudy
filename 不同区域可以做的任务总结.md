## Global区域

	能做的：
		
		1\.定义命名空间
		
		2\.定义类型（类、结构、枚举等）
		
		3\.顶级语句

		4\.导入命令空间（using System;等） 必须放在所有类型定义之前，作用是：在代码中可以直接使用 System 命名空间下的类型，而不需要每次都写完整路径。System.Console.WriteLine()可以简写成Console.WriteLine()

## 类内

	能做的：

		1\.变量初始化 实际上变量初始化是依靠构造函数完成的，类中唯一允许的值初始化语法是在声明时初始化，但它的实际作用是在每一个构造函数开头加上赋值语句，也就是值初始化是在构造函数内执行的，所以初始化的赋值运算可以进行

		2\.定义方法

		3\.定义常量

		4\.定义构造函数

		5\.定义属性

		6\.可以给出 public,[SerializeField] 等修饰

		7\.定义事件

		8\.定义索引器

	不能做的：

		1\.具体的运算，如赋值语句等

		2\.直接写可执行语句，但可以调用

	不报错但很奇怪的：

		1\.随机数生成，调用构造函数生成一个随机数，这是合法的，但是值并不随机，每一次都是0，猜测是unity保存了计算下来的固定值

## 方法内

	概述：

	能做的：
		
		1\.局部变量

		2\.局部常量

		3\.foreach等控制结构

		4\.异常处理

		5\.条件语句

		6\.可执行的运算代码

	不能做的：

		1\.不能定义一个类、结构、枚举、委托、接口
	
		2\.不能给变量增加[SerializeField]、private、public修饰

		3\.不能定义一个静态成员，如静态变量

		4\.不能定义字段